name: 'Outputs'
description: 'use output to next step'
inputs:
  script_path:
    description: 'script file path to dump output'
    required: true
  shell:
    description: 'shell to exec script file'
    require: false
    default: 'bash'
outputs:
  result:
    description: 'reslut to exec script'
runs:
  using: 'node12'
  main: 'main.js'
